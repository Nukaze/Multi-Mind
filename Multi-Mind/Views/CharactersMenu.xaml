<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Multi_Mind.Views.CharactersMenu"
             Title="Characters Menu">


    <VerticalStackLayout VerticalOptions="StartAndExpand" BackgroundColor="AliceBlue" HeightRequest="700">
        <Label 
            x:Name="TitleLabel"
            Text="" FontSize="48"
            Padding="60, 0, 60, 0"
            TextColor="Black"
            FontAttributes="Bold"
            HorizontalTextAlignment="Center"
            
           />
        <ScrollView 
            HeightRequest="600"
            Margin="20,0,20, 0" Padding="20, 20, 20, 20"
            >
            <VerticalStackLayout x:Name="buttonVertStackLayout" Spacing="10" >

                <!-- 
                    This section is adding the buttons dynamically from the backend
                -->

            </VerticalStackLayout>

        </ScrollView>
    </VerticalStackLayout>

</ContentPage>